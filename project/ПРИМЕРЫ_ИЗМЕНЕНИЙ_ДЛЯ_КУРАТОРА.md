# 🛠️ Практические примеры изменений для куратора

## Быстрые изменения, которые может попросить куратор

---

## 1️⃣ Изменить текст на странице

### Задача: "Измени заголовок на главной странице"

**Файл:** `templates/game/home.html`

**Было:**
```html
<h2>Мое портфолио</h2>
```

**Стало:**
```html
<h2>Галерея моих работ</h2>
```

---

## 2️⃣ Добавить новую секцию

### Задача: "Добавь секцию с преимуществами на главную"

**Файл:** `templates/game/home.html`

**Добавь перед `{% endblock %}`:**

```html
<div style="background: #e8f4f8; padding: 30px; border-radius: 8px; margin-top: 30px;">
    <h2>✨ Мои преимущества</h2>
    <div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px;">
        <div style="text-align: center;">
            <div style="font-size: 48px;">📸</div>
            <h3>Опыт</h3>
            <p>Более 5 лет в фотографии</p>
        </div>
        <div style="text-align: center;">
            <div style="font-size: 48px;">⚡</div>
            <h3>Скорость</h3>
            <p>Обработка за 3-5 дней</p>
        </div>
        <div style="text-align: center;">
            <div style="font-size: 48px;">💰</div>
            <h3>Цены</h3>
            <p>Доступные тарифы</p>
        </div>
    </div>
</div>
```

---

## 3️⃣ Добавить счетчик

### Задача: "Покажи количество отзывов"

**Файл:** `templates/game/reviews.html`

**Добавь после заголовка:**

```html
<h1>💬 Отзывы наших клиентов</h1>

<!-- НОВЫЙ КОД -->
<div style="background: #d4edda; padding: 15px; border-radius: 8px; margin-bottom: 20px;">
    <p style="margin: 0; font-size: 18px; color: #155724;">
        📊 Всего отзывов: <strong>{{ reviews|length }}</strong>
        {% if reviews %}
            | Средняя оценка: <strong>⭐ 4.8</strong>
        {% endif %}
    </p>
</div>
```

---

## 4️⃣ Изменить меню

### Задача: "Добавь ссылку на Instagram в меню"

**Файл:** `templates/game/base.html`

**Было:**
```html
<div class="menu">
    <a href="{% url 'home' %}">Главная</a>
    <a href="{% url 'services' %}">Услуги и цены</a>
    <a href="{% url 'reviews' %}">Отзывы</a>
    <a href="#contact">Контакты</a>
</div>
```

**Стало:**
```html
<div class="menu">
    <a href="{% url 'home' %}">Главная</a>
    <a href="{% url 'services' %}">Услуги и цены</a>
    <a href="{% url 'reviews' %}">Отзывы</a>
    <a href="#contact">Контакты</a>
    <a href="https://instagram.com/yourpage" target="_blank" 
       style="color: #e74c3c;">📷 Instagram</a>
</div>
```

---

## 5️⃣ Добавить кнопку "Позвонить"

### Задача: "Добавь кнопку для звонка на каждую страницу"

**Файл:** `templates/game/base.html`

**Добавь перед `</body>`:**

```html
<!-- Плавающая кнопка звонка -->
<a href="tel:+79991234567" 
   style="position: fixed; bottom: 20px; right: 20px; 
          background: #27ae60; color: white; 
          padding: 15px 25px; border-radius: 50px; 
          text-decoration: none; font-weight: bold; 
          box-shadow: 0 4px 8px rgba(0,0,0,0.2);
          z-index: 1000;">
    📞 Позвонить
</a>
</body>
```

---

## 6️⃣ Добавить фильтр отзывов

### Задача: "Покажи только отзывы с 5 звездами"

**Файл:** `templates/game/reviews.html`

**Добавь перед списком отзывов:**

```html
<div style="margin-bottom: 20px;">
    <button onclick="filterReviews('all')" class="button">Все</button>
    <button onclick="filterReviews('5')" class="button">⭐⭐⭐⭐⭐</button>
    <button onclick="filterReviews('4')" class="button">⭐⭐⭐⭐</button>
</div>

<script>
function filterReviews(rating) {
    const cards = document.querySelectorAll('.review-card');
    cards.forEach(card => {
        if (rating === 'all') {
            card.style.display = 'block';
        } else {
            const stars = card.querySelectorAll('.review-rating ⭐').length;
            card.style.display = stars == rating ? 'block' : 'none';
        }
    });
}
</script>
```

---

## 7️⃣ Добавить форму поиска

### Задача: "Добавь поиск по фотографиям"

**Файл:** `templates/game/home.html`

**Добавь перед галереей:**

```html
<div style="margin-bottom: 30px;">
    <form method="get" style="display: flex; gap: 10px;">
        <input type="text" name="search" placeholder="Поиск фотографий..." 
               value="{{ request.GET.search }}"
               style="flex: 1; padding: 10px; border: 1px solid #ddd; border-radius: 4px;">
        <button type="submit" class="button">🔍 Найти</button>
    </form>
</div>
```

**В `views.py` измени функцию `home`:**

```python
def home(request):
    photos = Photo.objects.all()
    
    # Поиск
    search = request.GET.get('search')
    if search:
        photos = photos.filter(title__icontains=search)
    
    return render(request, 'game/home.html', {'photos': photos})
```

---

## 8️⃣ Добавить хлебные крошки

### Задача: "Добавь навигационную цепочку"

**Файл:** `templates/game/reviews.html`

**Добавь после `{% block content %}`:**

```html
{% block content %}
<!-- Хлебные крошки -->
<div style="margin-bottom: 20px; color: #666;">
    <a href="{% url 'home' %}" style="color: #3498db;">Главная</a> 
    <span style="margin: 0 10px;">/</span>
    <span>Отзывы</span>
</div>

<h1>💬 Отзывы наших клиентов</h1>
```

---

## 9️⃣ Добавить модальное окно

### Задача: "Покажи фото в полном размере при клике"

**Файл:** `templates/game/home.html`

**Замени карточку фото:**

```html
<div class="photo-card">
    <img src="{{ photo.image.url }}" 
         alt="{{ photo.title }}"
         onclick="showModal('{{ photo.image.url }}')"
         style="cursor: pointer;">
    <h3>{{ photo.title }}</h3>
</div>
```

**Добавь перед `{% endblock %}`:**

```html
<!-- Модальное окно -->
<div id="photoModal" onclick="closeModal()" 
     style="display: none; position: fixed; top: 0; left: 0; 
            width: 100%; height: 100%; background: rgba(0,0,0,0.9); 
            z-index: 9999; cursor: pointer;">
    <img id="modalImage" src="" 
         style="position: absolute; top: 50%; left: 50%; 
                transform: translate(-50%, -50%); 
                max-width: 90%; max-height: 90%;">
    <span style="position: absolute; top: 20px; right: 40px; 
                 color: white; font-size: 40px; cursor: pointer;">
        ×
    </span>
</div>

<script>
function showModal(imageUrl) {
    document.getElementById('photoModal').style.display = 'block';
    document.getElementById('modalImage').src = imageUrl;
}

function closeModal() {
    document.getElementById('photoModal').style.display = 'none';
}
</script>
```

---

## 🔟 Добавить таймер акции

### Задача: "Покажи обратный отсчет до конца акции"

**Файл:** `templates/game/services.html`

**Добавь перед списком услуг:**

```html
<div style="background: #fff3cd; padding: 20px; border-radius: 8px; 
            margin-bottom: 30px; text-align: center;">
    <h2>🎉 Акция! Скидка 20% на все услуги</h2>
    <p style="font-size: 24px; font-weight: bold; color: #e74c3c;">
        До конца акции: <span id="timer"></span>
    </p>
</div>

<script>
// Установи дату окончания акции
const endDate = new Date('2024-12-31 23:59:59').getTime();

function updateTimer() {
    const now = new Date().getTime();
    const distance = endDate - now;
    
    const days = Math.floor(distance / (1000 * 60 * 60 * 24));
    const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
    const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
    const seconds = Math.floor((distance % (1000 * 60)) / 1000);
    
    document.getElementById('timer').innerHTML = 
        days + " дней " + hours + " часов " + minutes + " минут " + seconds + " секунд";
    
    if (distance < 0) {
        document.getElementById('timer').innerHTML = "Акция завершена";
    }
}

updateTimer();
setInterval(updateTimer, 1000);
</script>
```

---

## 1️⃣1️⃣ Добавить FAQ (Часто задаваемые вопросы)

### Задача: "Добавь раздел с вопросами и ответами"

**Создай новый файл:** `templates/game/faq.html`

```html
{% extends 'game/base.html' %}

{% block title %}Часто задаваемые вопросы{% endblock %}

{% block content %}
<h1>❓ Часто задаваемые вопросы</h1>

<div style="background: white; padding: 30px; border-radius: 8px;">
    <div style="margin-bottom: 30px;">
        <h3 style="color: #3498db;">Сколько стоит фотосессия?</h3>
        <p>Цены начинаются от 3000 рублей. Точная стоимость зависит от типа съемки и длительности.</p>
    </div>
    
    <div style="margin-bottom: 30px;">
        <h3 style="color: #3498db;">Как долго обрабатываются фотографии?</h3>
        <p>Обычно 3-5 рабочих дней. Срочная обработка - 1-2 дня за дополнительную плату.</p>
    </div>
    
    <div style="margin-bottom: 30px;">
        <h3 style="color: #3498db;">Сколько фотографий я получу?</h3>
        <p>В среднем 30-50 обработанных фотографий с часовой фотосессии.</p>
    </div>
    
    <div style="margin-bottom: 30px;">
        <h3 style="color: #3498db;">Можно ли выбрать место съемки?</h3>
        <p>Да, конечно! Мы можем провести съемку в студии, на природе или в любом удобном для вас месте.</p>
    </div>
</div>
{% endblock %}
```

**Добавь в `urls.py`:**
```python
path('faq/', views.faq, name='faq'),
```

**Добавь в `views.py`:**
```python
def faq(request):
    return render(request, 'game/faq.html')
```

**Добавь ссылку в меню:**
```html
<a href="{% url 'faq' %}">FAQ</a>
```

---

## 1️⃣2️⃣ Добавить карту

### Задача: "Покажи где находится студия"

**Файл:** `templates/game/base.html` (в футере)

**Замени футер:**

```html
<div class="footer">
    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 30px;">
        <div>
            <h3>Контакты</h3>
            <p>📞 Телефон: +7 (999) 123-45-67</p>
            <p>📧 Email: photo@example.com</p>
            <p>📍 Адрес: Москва, ул. Примерная, д. 1</p>
        </div>
        <div>
            <h3>Мы на карте</h3>
            <iframe src="https://yandex.ru/map-widget/v1/?ll=37.617644,55.755819&z=12" 
                    width="100%" height="200" frameborder="0" 
                    style="border-radius: 8px;">
            </iframe>
        </div>
    </div>
    <p style="margin-top: 20px;">© 2024 Фотограф Алексей</p>
</div>
```

---

## ✅ Шпаргалка: Где что менять

| Что нужно изменить | Какой файл открыть |
|-------------------|-------------------|
| Текст на главной | `templates/game/home.html` |
| Текст на странице услуг | `templates/game/services.html` |
| Текст на странице отзывов | `templates/game/reviews.html` |
| Меню сайта | `templates/game/base.html` |
| Шапка сайта | `templates/game/base.html` |
| Футер (подвал) | `templates/game/base.html` |
| Стили (цвета, размеры) | `static/css/styles.css` |
| Добавить новую страницу | Создать новый файл в `templates/game/` |

---

## 🎯 Алгоритм действий

Когда куратор просит что-то добавить:

1. **Определи где** - на какой странице нужно изменение
2. **Открой файл** - найди нужный HTML файл
3. **Найди место** - где вставить новый код
4. **Скопируй пример** - из этого документа
5. **Адаптируй** - измени текст под свои нужды
6. **Сохрани** - Ctrl+S
7. **Проверь** - обнови страницу в браузере (F5)

---

Теперь ты готов к любым изменениям! 🚀
