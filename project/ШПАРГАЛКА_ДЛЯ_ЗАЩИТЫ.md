# 🎓 Шпаргалка для защиты проекта

## Быстрые ответы на вопросы

### Что такое Django?
**Ответ:** Django - это веб-фреймворк на Python для быстрой разработки веб-приложений. Следует принципу MVT (Model-View-Template).

### Что такое MVT?
**Ответ:**
- **Model** (Модель) - работа с базой данных (`models.py`)
- **View** (Представление) - бизнес-логика (`views.py`)
- **Template** (Шаблон) - HTML страницы (`templates/`)

### Какие модели есть в проекте?
**Ответ:** 3 модели:
1. **Photo** - фотографии (title, image, preview, uploaded_at)
2. **Service** - услуги (name, description, price)
3. **Review** - отзывы (author_name, email, rating, text, created_at, is_approved)

### Как работает система отзывов?
**Ответ:**
1. Пользователь заполняет форму на странице `/reviews/`
2. Django проверяет данные через `ReviewForm`
3. Отзыв сохраняется с флагом `is_approved=False`
4. Модератор проверяет отзыв в админ-панели
5. После одобрения отзыв появляется на сайте

### Что такое миграции?
**Ответ:** Миграции - это инструкции для изменения структуры базы данных. Команды:
- `makemigrations` - создать инструкции
- `migrate` - применить изменения

### Зачем нужна модерация отзывов?
**Ответ:** Для защиты от спама, неприемлемого контента и фейковых отзывов.

### Что такое ORM?
**Ответ:** Object-Relational Mapping - работа с базой данных через Python объекты вместо SQL запросов.
Пример: `Review.objects.all()` вместо `SELECT * FROM review`

### Что делает {% csrf_token %}?
**Ответ:** Защищает от CSRF атак (подделки межсайтовых запросов). Это токен безопасности.

### Какая база данных используется?
**Ответ:** SQLite - легкая файловая БД, хранится в файле `db.sqlite3`

### Как добавить новую страницу?
**Ответ:** 4 шага:
1. Создать функцию в `views.py`
2. Добавить маршрут в `urls.py`
3. Создать шаблон в `templates/`
4. (Опционально) Добавить модель в `models.py`

---

## Структура проекта (кратко)

```
project/
├── manage.py          # Управление проектом
├── db.sqlite3         # База данных
├── project/           # Настройки
│   ├── settings.py    # Конфигурация
│   └── urls.py        # Главные маршруты
├── game/              # Приложение
│   ├── models.py      # Модели (БД)
│   ├── views.py       # Логика
│   ├── urls.py        # Маршруты
│   ├── forms.py       # Формы
│   └── admin.py       # Админка
├── templates/         # HTML шаблоны
├── static/            # CSS, JS
└── media/             # Загруженные файлы
```

---

## Основные команды

```bash
# Запуск сервера
python manage.py runserver

# Создание миграций
python manage.py makemigrations

# Применение миграций
python manage.py migrate

# Создание суперпользователя
python manage.py createsuperuser

# Запуск Python shell
python manage.py shell
```

---

## Код модели Review (объяснить на защите)

```python
class Review(models.Model):
    author_name = models.CharField(max_length=100)
    # CharField - текстовое поле с ограничением длины
    
    email = models.EmailField(blank=True, null=True)
    # EmailField - автоматическая валидация email
    # blank=True - поле необязательное
    
    rating = models.IntegerField(choices=[(1,'1'), (2,'2'), ...])
    # IntegerField - целое число
    # choices - ограничение значений (1-5)
    
    text = models.TextField()
    # TextField - длинный текст без ограничений
    
    created_at = models.DateTimeField(auto_now_add=True)
    # auto_now_add=True - автоматически при создании
    
    is_approved = models.BooleanField(default=False)
    # BooleanField - True/False
    # default=False - по умолчанию не одобрен
```

---

## Код view функции (объяснить на защите)

```python
def reviews(request):
    # Получаем одобренные отзывы
    approved_reviews = Review.objects.filter(is_approved=True)
    
    if request.method == 'POST':
        # Пользователь отправил форму
        form = ReviewForm(request.POST)
        if form.is_valid():
            # Данные корректны
            review = form.save(commit=False)
            review.is_approved = False  # Требует модерации
            review.save()
            messages.success(request, 'Спасибо!')
            return redirect('reviews')
    else:
        # Просто показываем страницу
        form = ReviewForm()
    
    return render(request, 'game/reviews.html', {
        'reviews': approved_reviews,
        'form': form
    })
```

---

## Возможные вопросы и ответы

**В: Почему используется SQLite?**
О: Легкая, не требует установки сервера БД, подходит для разработки и небольших проектов.

**В: Можно ли использовать другую БД?**
О: Да, Django поддерживает PostgreSQL, MySQL, Oracle. Меняется только настройка в `settings.py`.

**В: Что такое admin.py?**
О: Регистрация моделей в админ-панели Django. Дает готовый интерфейс для управления данными.

**В: Зачем нужен forms.py?**
О: Для создания и валидации форм. ModelForm автоматически создает форму из модели.

**В: Что такое static и media?**
О: 
- `static/` - статические файлы (CSS, JS, картинки сайта)
- `media/` - файлы, загруженные пользователями

**В: Как работает наследование шаблонов?**
О: `base.html` содержит общую структуру (меню, футер). Другие шаблоны наследуют через `{% extends %}` и заполняют блоки `{% block content %}`.

**В: Что такое context в render?**
О: Словарь с данными, которые передаются в шаблон. Например: `{'reviews': approved_reviews}`

**В: Зачем redirect после POST?**
О: Паттерн Post-Redirect-Get. Предотвращает повторную отправку формы при обновлении страницы.

**В: Что такое messages?**
О: Система сообщений Django для показа уведомлений пользователю (успех, ошибка и т.д.).

**В: Как защищен проект?**
О: 
- CSRF токены в формах
- Валидация данных через формы
- Модерация контента
- Встроенная защита Django от SQL-инъекций и XSS

---

## Демонстрация на защите

1. **Показать структуру проекта** - объяснить папки
2. **Открыть models.py** - объяснить модель Review
3. **Открыть views.py** - объяснить логику
4. **Показать сайт** - `/reviews/`
5. **Оставить тестовый отзыв** - показать, что он не появился
6. **Зайти в админку** - `/admin/`
7. **Одобрить отзыв** - поставить галочку
8. **Обновить сайт** - отзыв появился!

---

## Что можно улучшить (если спросят)

1. Добавить капчу от ботов
2. Добавить возможность ответа на отзывы
3. Добавить фото к отзывам
4. Добавить уведомления на email
5. Добавить пагинацию (разбивку на страницы)
6. Добавить сортировку отзывов
7. Использовать PostgreSQL вместо SQLite
8. Добавить API для мобильного приложения

---

## Главное помнить

✅ Django работает по принципу MVT
✅ Модели описывают структуру данных
✅ Views обрабатывают запросы
✅ Templates показывают данные пользователю
✅ Миграции изменяют структуру БД
✅ Формы проверяют и сохраняют данные
✅ Админка дает готовый интерфейс управления

**Удачи на защите! 🎓**
