# 📚 Подробное объяснение системы отзывов

## Что было реализовано

Добавлена полноценная система отзывов с модерацией для сайта фотографа.

---

## 🏗️ Архитектура решения (MVT паттерн Django)

### 1. MODEL (Модель данных) - `models.py`

**Класс Review** - описывает структуру отзыва в базе данных:

```python
class Review(models.Model):
    author_name = models.CharField(max_length=100)  # Имя автора
    email = models.EmailField(blank=True)           # Email (необязательно)
    rating = models.IntegerField(choices=...)       # Оценка 1-5
    text = models.TextField()                       # Текст отзыва
    created_at = models.DateTimeField(auto_now_add=True)  # Дата создания
    is_approved = models.BooleanField(default=False)      # Модерация
```

**Зачем каждое поле:**
- `author_name` - чтобы знать, кто оставил отзыв
- `email` - для связи (необязательно, поэтому `blank=True`)
- `rating` - оценка от 1 до 5 звезд
- `text` - сам отзыв
- `created_at` - автоматически сохраняет дату/время создания
- `is_approved` - флаг модерации (False = отзыв скрыт, True = показывается)

**Meta класс:**
- `ordering = ['-created_at']` - сортировка: новые отзывы сверху
- `verbose_name` - русское название для админки

---

### 2. FORM (Форма) - `forms.py`

**Класс ReviewForm** - создает HTML форму для пользователя:

```python
class ReviewForm(forms.ModelForm):
    class Meta:
        model = Review
        fields = ['author_name', 'email', 'rating', 'text']
```

**ModelForm** - это специальный класс Django, который:
- Автоматически создает поля формы на основе модели
- Проверяет данные (валидация)
- Сохраняет данные в базу

**widgets** - настройка внешнего вида полей:
- Добавляет CSS классы (`class='form-control'`)
- Добавляет placeholder (подсказки в полях)
- Настраивает размер textarea

---

### 3. VIEW (Представление) - `views.py`

**Функция reviews()** - обрабатывает запросы к странице отзывов:

```python
def reviews(request):
    # 1. Получаем одобренные отзывы
    approved_reviews = Review.objects.filter(is_approved=True)
    
    # 2. Проверяем тип запроса
    if request.method == 'POST':  # Пользователь отправил форму
        form = ReviewForm(request.POST)
        if form.is_valid():  # Данные корректны?
            review = form.save(commit=False)  # Создаем объект, но не сохраняем
            review.is_approved = False  # Отзыв требует модерации
            review.save()  # Теперь сохраняем в БД
            messages.success(request, 'Спасибо за отзыв!')
            return redirect('reviews')  # Перенаправляем на эту же страницу
    else:  # GET запрос - просто показываем страницу
        form = ReviewForm()
    
    # 3. Передаем данные в шаблон
    return render(request, 'game/reviews.html', {
        'reviews': approved_reviews,
        'form': form
    })
```

**Логика работы:**

**GET запрос** (пользователь открыл страницу):
1. Получить одобренные отзывы из БД
2. Создать пустую форму
3. Показать страницу

**POST запрос** (пользователь отправил форму):
1. Получить данные из формы
2. Проверить корректность (`is_valid()`)
3. Сохранить с флагом `is_approved=False`
4. Показать сообщение об успехе
5. Перенаправить (чтобы форма очистилась)

---

### 4. URL (Маршрутизация) - `urls.py`

```python
path('reviews/', views.reviews, name='reviews')
```

**Что это значит:**
- Когда пользователь заходит на `/reviews/`
- Django вызывает функцию `views.reviews`
- `name='reviews'` - имя для использования в шаблонах

---

### 5. TEMPLATE (Шаблон) - `reviews.html`

HTML страница с:
- Списком отзывов (цикл `{% for review in reviews %}`)
- Формой для добавления отзыва
- Отображением звездочек рейтинга
- Сообщениями об успехе

**Важные Django теги:**

```django
{% extends 'game/base.html' %}  - наследование от базового шаблона
{% block content %} - вставка контента в блок
{% csrf_token %} - защита от CSRF атак
{% for review in reviews %} - цикл по отзывам
{{ review.author_name }} - вывод переменной
{% url 'reviews' %} - генерация URL по имени
```

---

### 6. ADMIN (Админ-панель) - `admin.py`

```python
@admin.register(Review)
class ReviewAdmin(admin.ModelAdmin):
    list_display = ['author_name', 'rating', 'is_approved', 'created_at']
    list_editable = ['is_approved']
    list_filter = ['is_approved', 'rating']
    search_fields = ['author_name', 'text']
```

**Возможности:**
- `list_display` - какие колонки показывать в списке
- `list_editable` - какие поля можно редактировать прямо в списке
- `list_filter` - фильтры справа (по статусу, рейтингу)
- `search_fields` - поиск по имени и тексту

---

## 🔄 Полный цикл работы (пример)

### Сценарий 1: Пользователь просматривает отзывы

1. **Браузер**: GET запрос на `/reviews/`
2. **urls.py**: находит маршрут, вызывает `views.reviews`
3. **views.py**: 
   - Получает отзывы: `Review.objects.filter(is_approved=True)`
   - Создает пустую форму: `ReviewForm()`
4. **Template**: отображает отзывы и форму
5. **Браузер**: показывает страницу пользователю

### Сценарий 2: Пользователь оставляет отзыв

1. **Браузер**: пользователь заполняет форму и нажимает "Отправить"
2. **Браузер**: POST запрос на `/reviews/` с данными формы
3. **urls.py**: вызывает `views.reviews`
4. **views.py**:
   - Получает данные: `ReviewForm(request.POST)`
   - Проверяет: `form.is_valid()`
   - Сохраняет: `review.save()`
   - Перенаправляет: `redirect('reviews')`
5. **Браузер**: показывает обновленную страницу с сообщением

### Сценарий 3: Модератор одобряет отзыв

1. **Админ**: заходит в `/admin/`
2. **Админ**: открывает раздел "Отзывы"
3. **Админ**: ставит галочку "Одобрен" и сохраняет
4. **База данных**: `is_approved` меняется на `True`
5. **Сайт**: отзыв теперь виден всем пользователям

---

## 🛡️ Безопасность

### CSRF защита
```django
{% csrf_token %}
```
Защищает от подделки межсайтовых запросов.

### Валидация данных
```python
if form.is_valid():
```
Django автоматически проверяет:
- Правильность email
- Длину полей
- Обязательность полей

### Модерация
```python
is_approved = models.BooleanField(default=False)
```
Отзывы не публикуются автоматически - нужно одобрение.

---

## 📊 База данных

### Миграции

**Что такое миграции?**
Миграции - это инструкции для изменения структуры базы данных.

**Команды:**
```bash
python manage.py makemigrations  # Создать инструкции
python manage.py migrate         # Применить изменения
```

**Что происходит:**
1. Django анализирует изменения в `models.py`
2. Создает файл миграции (например, `0002_review.py`)
3. Применяет изменения к базе данных (создает таблицу)

**Структура таблицы в БД:**
```
game_review
├── id (автоматически)
├── author_name
├── email
├── rating
├── text
├── created_at
└── is_approved
```

---

## 🎯 Ключевые концепции для защиты

### 1. MVT паттерн
- **Model** - данные (что хранить)
- **View** - логика (как обработать)
- **Template** - отображение (как показать)

### 2. ORM (Object-Relational Mapping)
Работа с БД через Python объекты:
```python
Review.objects.all()  # SELECT * FROM game_review
Review.objects.filter(is_approved=True)  # WHERE is_approved=True
review.save()  # INSERT или UPDATE
```

### 3. Формы Django
- Автоматическая генерация HTML
- Валидация данных
- Защита от атак

### 4. Система шаблонов
- Наследование (`{% extends %}`)
- Переменные (`{{ variable }}`)
- Теги (`{% for %}`, `{% if %}`)

### 5. URL маршрутизация
- Связь URL с функциями
- Именованные маршруты
- Параметры в URL

---

## 💡 Возможные вопросы на защите

**В: Почему отзывы не публикуются сразу?**
О: Для защиты от спама и неприемлемого контента. Модератор проверяет каждый отзыв.

**В: Что такое csrf_token?**
О: Защита от CSRF атак. Это уникальный токен, который проверяет, что форма отправлена с нашего сайта.

**В: Зачем commit=False в save()?**
О: Чтобы создать объект, но не сохранять его сразу. Мы можем изменить поля (например, is_approved) перед сохранением.

**В: Что делает filter(is_approved=True)?**
О: Выбирает из базы только те отзывы, где поле is_approved равно True.

**В: Зачем redirect после POST?**
О: Паттерн Post-Redirect-Get. Предотвращает повторную отправку формы при обновлении страницы.

---

## 🚀 Как запустить и протестировать

1. Запустить сервер:
```bash
cd project
python manage.py runserver
```

2. Открыть страницу отзывов:
```
http://localhost:8000/reviews/
```

3. Оставить тестовый отзыв

4. Зайти в админку и одобрить:
```
http://localhost:8000/admin/
```

5. Проверить, что отзыв появился на сайте

---

## ✅ Что получилось

✓ Модель для хранения отзывов
✓ Форма для добавления отзывов
✓ Страница с отображением отзывов
✓ Система модерации
✓ Рейтинг со звездочками
✓ Красивый дизайн
✓ Защита от спама
✓ Админ-панель для управления

Удачи на защите! 🎓
